# Environment variables template for Auth0 middleware

# Copy this file to .env and fill in your Auth0 application credentials

# Auth0 Configuration (Required)
AUTH0_DOMAIN=your-tenant.auth0.com
AUTH0_CLIENT_ID=your_auth0_client_id_here
AUTH0_CLIENT_SECRET=your_auth0_client_secret_here

# Session Security (Required)
# Generate a random 32+ character string for production
# Example: openssl rand -base64 32
SESSION_SECRET=your_random_session_secret_at_least_32_characters_long

# Application URLs (Optional - defaults provided)
# AUTH0_CALLBACK_URL=http://localhost:8080/auth/callback
# AUTH0_LOGOUT_URL=http://localhost:8080

# Production-specific settings
# For HTTPS deployments
# AUTH0_CALLBACK_URL=https://yourdomain.com/auth/callback
# AUTH0_LOGOUT_URL=https://yourdomain.com
